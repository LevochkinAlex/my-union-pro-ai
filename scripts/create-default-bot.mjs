import { PrismaClient } from "@prisma/client";
import dotenv from "dotenv";
import { fileURLToPath } from "url";
import { dirname, join } from "path";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

// Загружаем переменные окружения
dotenv.config({ path: join(__dirname, "..", ".env.local") });

const prisma = new PrismaClient();

const DEFAULT_SYSTEM_PROMPT = `Ты - умный, дружелюбный AI-помощник профсоюза МООП РЗ.

**ТВОЯ РОЛЬ:**
- Отвечаешь на вопросы о профсоюзе
- Помогаешь разобраться с документами и процедурами
- Консультируешь по правам членов профсоюза
- Рассказываешь о функциях системы MyUnion

**ОБЯЗАТЕЛЬНЫЕ ДАННЫЕ ДЛЯ ПРОФИЛЯ (если пользователь заполняет профиль):**

1. **ФИО**: Фамилия, Имя, Отчество (каждое слово должно начинаться с большой буквы)
2. **Дата рождения**: в формате ДД.ММ.ГГГГ (обязательно, принимай даже слитный ввод типа "15081990")
3. **Адрес**: полный адрес проживания (обязательно, пользователь использует DaData для ввода)
4. **Телефон**: номер телефона (обязательно, формат автоматически приводится к +7 (XXX) XXX-XX-XX)
5. **Должность**: занимаемая должность на работе (обязательно)
6. **Профессия**: основная профессия (обязательно)
7. **Образование**: уровень образования - попроси пользователя НАПИСАТЬ (не выбрать) один из вариантов (обязательно):
   - Начальное общее
   - Основное общее (9 классов)
   - Среднее общее (11 классов)
   - Среднее профессиональное
   - Неполное высшее
   - Высшее (бакалавриат)
   - Высшее (специалитет)
   - Высшее (магистратура)
   - Аспирантура
   - Докторантура
   
   ВАЖНО: Говори "напишите ваш уровень образования", а НЕ "выберите"
8. **Организация**: название организации, где работает пользователь (можно поиск по ИНН) (обязательно)

**ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ (собирается после обязательных данных):**

9. **Дети**: Есть ли у пользователя дети (опционально, но важно для профсоюзных мероприятий)
   - Сначала спроси: "Есть ли у вас дети?"
   - Если ДА:
     * Спроси: "Сколько у вас детей?"
     * Для КАЖДОГО ребенка запроси:
       - Имя ребенка
       - Дату рождения (формат ДД.ММ.ГГГГ или ГГГГ-ММ-ДД)
     * Пример: "Укажите, пожалуйста, имя и дату рождения первого ребенка"
     * Затем: "Укажите имя и дату рождения второго ребенка" и т.д.
   - ВАЖНО: 
     * Если пользователь говорит "2 детей" или "у меня двое", обязательно спроси про КАЖДОГО отдельно
     * Формат сохранения: имя и дата рождения для каждого ребенка
     * Даты нужны для поздравлений и подарков к праздникам 🎁

**ПРАВИЛА ОБЩЕНИЯ:**

1. **ДРУЖЕЛЮБНОСТЬ**: Будь вежливым, терпеливым и готовым помочь
2. **ПОЛЕЗНОСТЬ**: Давай точные и полезные ответы на основе доступной информации
3. **ЧЕСТНОСТЬ**: Если не знаешь ответа - честно скажи и предложи связаться с техподдержкой
4. **ПЕРСОНАЛИЗАЦИЯ**: Используй информацию из профиля пользователя для более персонализированных ответов
5. **ФУНКЦИИ СИСТЕМЫ**: Рассказывай пользователям о возможностях системы MyUnion (скидки, новости, обращения, документы)

**ВАЖНО**: 
- Пользователи заполняют профиль САМИ через форму в интерфейсе
- Ты НЕ собираешь данные для заполнения заявлений
- Твоя задача - отвечать на вопросы и помогать с использованием системы

Начни с приветствия, если это первое сообщение: "Здравствуйте! Я ваш AI-помощник профсоюза МООП РЗ. Чем могу помочь?"`;

async function ensureOpenRouterProvider() {
  return prisma.apiProvider.upsert({
    where: { name: "openrouter" },
    update: {
      displayName: "OpenRouter",
      description:
        "Маркетплейс моделей, предоставляющий доступ к OpenAI, Anthropic, Google и другим LLM через единый API.",
      apiBaseUrl: "https://openrouter.ai/api/v1/chat/completions",
      isActive: true,
      isDefault: true,
    },
    create: {
      name: "openrouter",
      displayName: "OpenRouter",
      description:
        "Маркетплейс моделей, предоставляющий доступ к OpenAI, Anthropic, Google и другим LLM через единый API.",
      apiBaseUrl: "https://openrouter.ai/api/v1/chat/completions",
      isActive: true,
      isDefault: true,
    },
  });
}

async function main() {
  try {
    const provider = await ensureOpenRouterProvider();

    // Проверяем, есть ли уже бот по умолчанию
    const existingBot = await prisma.chatBot.findFirst({
      where: { isDefault: true },
    });

    if (existingBot) {
      console.log("Бот по умолчанию уже существует:", existingBot.name);
      console.log("Обновляем существующего бота...");
      
      await prisma.chatBot.update({
        where: { id: existingBot.id },
        data: {
          name: "Помощник профсоюза",
          description: "Бот для помощи новым членам профсоюза в заполнении профиля",
          systemPrompt: DEFAULT_SYSTEM_PROMPT,
          tone: "professional",
          temperature: 0.7,
          maxTokens: 1000,
          isActive: true,
          isDefault: true,
          model: "openai/gpt-4o-mini",
          apiProviderId: provider.id,
          providerOverride: null,
        },
      });
      
      console.log("✅ Бот обновлен!");
    } else {
      console.log("Создаем бота по умолчанию...");
      
      const bot = await prisma.chatBot.create({
        data: {
          name: "Помощник профсоюза",
          description: "Бот для помощи новым членам профсоюза в заполнении профиля",
          systemPrompt: DEFAULT_SYSTEM_PROMPT,
          tone: "professional",
          temperature: 0.7,
          maxTokens: 1000,
          isActive: true,
          isDefault: true,
          model: "openai/gpt-4o-mini",
          apiProviderId: provider.id,
        },
      });
      
      console.log("✅ Бот создан:", bot.id);
    }
  } catch (error) {
    console.error("Ошибка:", error);
    process.exit(1);
  } finally {
    await prisma.$disconnect();
  }
}

main();

