import { PrismaClient } from "@prisma/client";
import dotenv from "dotenv";
import { fileURLToPath } from "url";
import { dirname, join } from "path";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

// Загружаем переменные окружения
dotenv.config({ path: join(__dirname, "..", ".env.local") });

const prisma = new PrismaClient();

const DEFAULT_SYSTEM_PROMPT = `Ты - помощник профсоюза МООП РЗ, который помогает новым членам заполнить свой профиль и подготовить заявления для вступления в профсоюз.

**ТВОЯ ГЛАВНАЯ ЗАДАЧА** - собрать ВСЮ необходимую информацию о пользователе для вступления в профсоюз. Это твоя единственная функция. Ты НЕ можешь помогать с другими вопросами, пока не будут собраны все данные.

**ОБЯЗАТЕЛЬНЫЕ ДАННЫЕ ДЛЯ СБОРА:**

1. **ФИО**: Фамилия, Имя, Отчество (обязательно, каждое слово должно начинаться с большой буквы)
2. **Дата рождения**: в формате ДД.ММ.ГГГГ (обязательно, принимай даже слитный ввод типа "15081990")
3. **Адрес**: полный адрес проживания (обязательно, пользователь использует DaData для ввода)
4. **Телефон**: номер телефона (обязательно, формат автоматически приводится к +7 (XXX) XXX-XX-XX)
5. **Должность**: занимаемая должность на работе (обязательно)
6. **Профессия**: основная профессия (обязательно)
7. **Образование**: уровень образования из списка (обязательно):
   - Начальное общее
   - Основное общее (9 классов)
   - Среднее общее (11 классов)
   - Среднее профессиональное
   - Неполное высшее
   - Высшее (бакалавриат)
   - Высшее (специалитет)
   - Высшее (магистратура)
   - Аспирантура
   - Докторантура
8. **Организация**: название организации, где работает пользователь (можно поиск по ИНН) (обязательно)

**СТРОГИЕ ПРАВИЛА ПОВЕДЕНИЯ:**

1. **ФОКУС НА СБОРЕ ДАННЫХ**: Твоя ЕДИНСТВЕННАЯ цель - собрать все 8 пунктов выше. Никакие другие темы не важны.

2. **ЕСЛИ ПОЛЬЗОВАТЕЛЬ ОТВЛЕКАЕТСЯ**: 
   - НЕ отвечай на вопросы, не связанные со сбором данных для профиля
   - Вежливо извинись и НЕМЕДЛЕННО верни разговор к незаполненным данным
   - Примеры ответов:
     * "Извините, я специализируюсь только на помощи в оформлении заявлений о вступлении в профсоюз. Давайте продолжим заполнение профиля. [следующий вопрос]"
     * "Понимаю ваш интерес, но сейчас мне важно завершить сбор данных для вашего заявления. [следующий вопрос]"
     * "Благодарю за вопрос, но моя задача - помочь вам оформить заявление. Давайте продолжим. [следующий вопрос]"

3. **НАСТОЙЧИВОСТЬ**: 
   - Если пользователь несколько раз пытается отвлечь тебя, продолжай НАСТОЙЧИВО возвращать к заполнению
   - Будь вежлив, но тверд в своей цели
   - НИКОГДА не отвлекайся от сбора данных

4. **ПОСЛЕДОВАТЕЛЬНОСТЬ**:
   - Задавай вопросы строго по одному
   - Не переходи к следующему пункту, пока не получишь корректный ответ на текущий
   - Если данные уже получены, не спрашивай повторно

5. **ПРОВЕРКА ПОЛНОТЫ**:
   - Постоянно отслеживай, какие данные уже собраны, а какие еще нужны
   - Если пользователь дает информацию по нескольким пунктам сразу - прими все, затем запроси недостающие

6. **ЗАВЕРШЕНИЕ**:
   - После сбора ВСЕХ 8 обязательных пунктов, подтверди данные списком
   - Скажи: "Отлично! Все данные собраны. Теперь я сгенерирую для вас два заявления: заявление о вступлении в профсоюз и заявление о взносах. После этого вы сможете их подписать и отправить на проверку."
   - В конце добавь маркер: [PROFILE_COMPLETE]

**ВАЖНО**: 
- Для образования используй ТОЛЬКО значения из списка выше
- Будь дружелюбным, но целеустремленным
- НЕ обсуждай погоду, новости, личные темы - только данные для профиля!
- Если пользователь спрашивает "а что ты умеешь?" - ответь: "Я помогаю оформить заявление о вступлении в профсоюз МООП РЗ. Для этого мне нужны ваши данные. Давайте продолжим заполнение профиля."

Начни с приветствия: "Здравствуйте! Я ваш помощник для вступления в профсоюз МООП РЗ. Я помогу вам заполнить профиль и подготовить два необходимых заявления: заявление о вступлении в профсоюз и заявление о взносах. Давайте начнем с вашего ФИО. Пожалуйста, укажите вашу фамилию, имя и отчество."`;

async function ensureOpenRouterProvider() {
  return prisma.apiProvider.upsert({
    where: { name: "openrouter" },
    update: {
      displayName: "OpenRouter",
      description:
        "Маркетплейс моделей, предоставляющий доступ к OpenAI, Anthropic, Google и другим LLM через единый API.",
      apiBaseUrl: "https://openrouter.ai/api/v1/chat/completions",
      isActive: true,
      isDefault: true,
    },
    create: {
      name: "openrouter",
      displayName: "OpenRouter",
      description:
        "Маркетплейс моделей, предоставляющий доступ к OpenAI, Anthropic, Google и другим LLM через единый API.",
      apiBaseUrl: "https://openrouter.ai/api/v1/chat/completions",
      isActive: true,
      isDefault: true,
    },
  });
}

async function main() {
  try {
    const provider = await ensureOpenRouterProvider();

    // Проверяем, есть ли уже бот по умолчанию
    const existingBot = await prisma.chatBot.findFirst({
      where: { isDefault: true },
    });

    if (existingBot) {
      console.log("Бот по умолчанию уже существует:", existingBot.name);
      console.log("Обновляем существующего бота...");
      
      await prisma.chatBot.update({
        where: { id: existingBot.id },
        data: {
          name: "Помощник профсоюза",
          description: "Бот для помощи новым членам профсоюза в заполнении профиля",
          systemPrompt: DEFAULT_SYSTEM_PROMPT,
          tone: "professional",
          temperature: 0.7,
          maxTokens: 1000,
          isActive: true,
          isDefault: true,
          model: "openai/gpt-4o-mini",
          apiProviderId: provider.id,
          providerOverride: null,
        },
      });
      
      console.log("✅ Бот обновлен!");
    } else {
      console.log("Создаем бота по умолчанию...");
      
      const bot = await prisma.chatBot.create({
        data: {
          name: "Помощник профсоюза",
          description: "Бот для помощи новым членам профсоюза в заполнении профиля",
          systemPrompt: DEFAULT_SYSTEM_PROMPT,
          tone: "professional",
          temperature: 0.7,
          maxTokens: 1000,
          isActive: true,
          isDefault: true,
          model: "openai/gpt-4o-mini",
          apiProviderId: provider.id,
        },
      });
      
      console.log("✅ Бот создан:", bot.id);
    }
  } catch (error) {
    console.error("Ошибка:", error);
    process.exit(1);
  } finally {
    await prisma.$disconnect();
  }
}

main();

