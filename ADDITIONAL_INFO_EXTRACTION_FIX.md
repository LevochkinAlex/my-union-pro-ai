# ✅ Исправление экстракта дополнительной информации профиля

> **Дата:** 24 ноября 2025  
> **Проблема:** Дополнительные поля профиля (занятость, семейное положение, супруг(а), дети, хобби, о себе) не сохранялись в базу данных

---

## 🐛 Описание проблемы

### Что происходило:

Пользователь отвечал на все дополнительные вопросы бота после генерации документов:
- ✅ Бот: "Укажите вашу занятость: работа, учеба или пенсия?"
- ✅ Пользователь: "Работаю"
- ✅ Бот: "Каково ваше семейное положение?"
- ✅ Пользователь: "Женат"
- ✅ Бот: "Расскажите о вашем супруге..."
- ✅ Пользователь: "Котлова Елена Юрьевна, поет песни"
- ✅ Бот: "Есть ли у вас дети?"
- ✅ Пользователь: "1"
- ✅ Бот: "Имя и дата рождения ребенка?"
- ✅ Пользователь: "Рома 01 07 2000"
- ✅ Бот: "Какие у вас хобби?"
- ✅ Пользователь: "Играю на гитаре"
- ✅ Бот: "Расскажите о себе..."
- ✅ Пользователь: "Люблю ковыряться в носу"

НО! В профиле все эти поля были **пустые** ❌

---

## 🔍 Анализ причин

### Причина 1: Неправильные паттерны экстракта

**Было:**
```typescript
// Старый подход: ищем структурированный формат
const hobbiesPatterns = [
  /(?:хобби|увлечения|увлекаюсь|интересы)[:\s]+([^\n.!?]{5,300})/i,
];
```

Этот паттерн требовал формат типа:
```
Хобби: Играю на гитаре
```

Но пользователь просто отвечал на вопрос бота:
```
BOT: Какие у вас хобби?
USER: Играю на гитаре
```

**Паттерн не срабатывал!** ❌

---

### Причина 2: Отсутствовала валидация полей

В функции `validateExtractedProfile` была валидация только для **основных** полей:
- ✅ firstName, lastName, middleName
- ✅ dateOfBirth, phone, address
- ✅ jobTitle, profession, education

НО отсутствовала валидация для **дополнительных** полей:
- ❌ employmentStatus
- ❌ maritalStatus
- ❌ spouseInfo
- ❌ hasChildren
- ❌ childrenInfo
- ❌ childrenBirthDates
- ❌ hobbies
- ❌ aboutMe
- ❌ additionalInfo

Из-за этого даже если данные извлекались, они отклонялись валидацией!

---

## ✅ Решение

### 1. Переписан подход экстракта: Контекстный анализ диалога

**Новый подход:**
Вместо поиска структурированных данных, анализируем **диалог**:

```typescript
// 🔥 НОВЫЙ ПОДХОД: Контекстный анализ диалога (вопрос бота -> ответ пользователя)
for (let i = 0; i < messages.length - 1; i++) {
  const currentMsg = messages[i];
  const nextMsg = messages[i + 1];
  
  // Пропускаем если это не диалог бот->пользователь
  if (currentMsg.role !== 'assistant' || nextMsg.role !== 'user') continue;
  
  const botQuestion = currentMsg.content.toLowerCase();
  const userAnswer = nextMsg.content.trim();
  
  // ЗАНЯТОСТЬ (employmentStatus)
  if (botQuestion.includes('занятость') && botQuestion.includes('работа')) {
    const answer = userAnswer.toLowerCase();
    if (answer.includes('работа') || answer.includes('работаю')) {
      profileData.employmentStatus = 'WORK';
    }
    // ... и т.д.
  }
  
  // ХОББИ (hobbies)
  if (botQuestion.includes('хобби') || botQuestion.includes('увлечения')) {
    if (userAnswer.length > 3 && !userAnswer.toLowerCase().includes('нет')) {
      profileData.hobbies = userAnswer; // ✅ Просто сохраняем ответ!
    }
  }
}
```

**Ключевая идея:**
1. Бот спрашивает: "Какие у вас хобби?"
2. Пользователь отвечает: "Играю на гитаре"
3. Система понимает контекст и сохраняет ответ в поле `hobbies`

---

### 2. Добавлена валидация для всех дополнительных полей

```typescript
// ========== ВАЛИДАЦИЯ ДОПОЛНИТЕЛЬНЫХ ПОЛЕЙ ==========

// Валидация employmentStatus
if (data.employmentStatus && typeof data.employmentStatus === 'string') {
  const status = data.employmentStatus.toUpperCase();
  if (['WORK', 'STUDY', 'RETIREMENT'].includes(status)) {
    validated.employmentStatus = status;
  }
}

// Валидация maritalStatus
if (data.maritalStatus && typeof data.maritalStatus === 'string') {
  const status = data.maritalStatus.toUpperCase();
  if (['SINGLE', 'MARRIED', 'DIVORCED', 'WIDOWED', 'CIVIL_MARRIAGE'].includes(status)) {
    validated.maritalStatus = status;
  }
}

// Валидация spouseInfo
if (data.spouseInfo && typeof data.spouseInfo === 'string') {
  const info = data.spouseInfo.trim();
  if (info.length >= 2 && info.length <= 500) {
    validated.spouseInfo = info;
  }
}

// Валидация hasChildren
if (data.hasChildren !== undefined && typeof data.hasChildren === 'boolean') {
  validated.hasChildren = data.hasChildren;
}

// Валидация childrenInfo
if (data.childrenInfo && typeof data.childrenInfo === 'string') {
  const info = data.childrenInfo.trim();
  if (info.length >= 2 && info.length <= 1000) {
    validated.childrenInfo = info;
  }
}

// Валидация childrenBirthDates
if (data.childrenBirthDates && typeof data.childrenBirthDates === 'string') {
  const dates = data.childrenBirthDates.trim();
  if (dates.length >= 2 && dates.length <= 2000) {
    validated.childrenBirthDates = dates;
  }
}

// Валидация hobbies
if (data.hobbies && typeof data.hobbies === 'string') {
  const hobbies = data.hobbies.trim();
  if (hobbies.length >= 2 && hobbies.length <= 1000) {
    validated.hobbies = hobbies;
  }
}

// Валидация aboutMe
if (data.aboutMe && typeof data.aboutMe === 'string') {
  const about = data.aboutMe.trim();
  if (about.length >= 2 && about.length <= 2000) {
    validated.aboutMe = about;
  }
}

// Валидация additionalInfo
if (data.additionalInfo && typeof data.additionalInfo === 'string') {
  const info = data.additionalInfo.trim();
  if (info.length >= 2 && info.length <= 2000) {
    validated.additionalInfo = info;
  }
}
```

---

## 📊 Примеры работы

### ✅ Пример 1: Занятость

**Диалог:**
```
BOT: Укажите вашу занятость: работа, учеба или пенсия?
USER: Работаю
```

**Сохраняется:**
```json
{
  "employmentStatus": "WORK"
}
```

---

### ✅ Пример 2: Семейное положение и супруг(а)

**Диалог:**
```
BOT: Каково ваше семейное положение?
USER: Женат

BOT: Расскажите о вашем супруге...
USER: Котлова Елена Юрьевна, поет песни
```

**Сохраняется:**
```json
{
  "maritalStatus": "MARRIED",
  "spouseInfo": "Котлова Елена Юрьевна, поет песни"
}
```

---

### ✅ Пример 3: Дети с датами рождения

**Диалог:**
```
BOT: Есть ли у вас дети?
USER: 1

BOT: Сколько у вас детей?
USER: 1

BOT: Укажите имя и дату рождения ребенка
USER: Рома 01 07 2000
```

**Сохраняется:**
```json
{
  "hasChildren": true,
  "childrenInfo": "Рома (01.07.2000)",
  "childrenBirthDates": "[{\"name\":\"Рома\",\"birthDate\":\"2000-07-01\",\"gender\":\"М\"}]"
}
```

**Формат JSON** для `childrenBirthDates`:
```json
[
  {
    "name": "Рома",
    "birthDate": "2000-07-01",
    "gender": "М"
  }
]
```

Этот формат нужен для:
- 🎁 Отправки подарков детям на дни рождения
- 📊 Аналитики по возрасту детей членов профсоюза
- 🎉 Автоматической рассылки поздравлений

---

### ✅ Пример 4: Хобби и о себе

**Диалог:**
```
BOT: Какие у вас хобби и увлечения?
USER: Играю на гитаре

BOT: Расскажите о себе: характер, привычки...
USER: Люблю ковыряться в носу
```

**Сохраняется:**
```json
{
  "hobbies": "Играю на гитаре",
  "aboutMe": "Люблю ковыряться в носу"
}
```

---

## 🎯 Поддерживаемые значения

### employmentStatus (Занятость)

| Значение | Описание | Триггеры |
|----------|----------|----------|
| `WORK` | Работа | "работаю", "работа", "трудоустроен" |
| `STUDY` | Учеба | "учусь", "учеба", "студент" |
| `RETIREMENT` | Пенсия | "пенсия", "на пенсии", "пенсионер" |

---

### maritalStatus (Семейное положение)

| Значение | Описание | Триггеры |
|----------|----------|----------|
| `SINGLE` | Холост/Не замужем | "холост", "не замужем", "не женат" |
| `MARRIED` | Женат/Замужем | "женат", "замужем", "в браке" |
| `DIVORCED` | В разводе | "развод", "разведен", "разведена" |
| `WIDOWED` | Вдовец/Вдова | "вдовец", "вдова" |
| `CIVIL_MARRIAGE` | Гражданский брак | "гражданск", "сожительство" |

---

## 🔧 Технические изменения

### Измененные файлы:

| Файл | Что изменено |
|------|--------------|
| `lib/profile-extraction.ts` | ✅ Переписан подход экстракта с паттернов на контекстный анализ диалога |
| `lib/profile-extraction.ts` | ✅ Добавлена валидация для всех дополнительных полей |
| `ADDITIONAL_INFO_EXTRACTION_FIX.md` | ✅ Создана документация (этот файл) |

---

## 📝 Результат для тестового пользователя

**Email:** 9061109990@mail.ru  
**Сессия:** cmidklte800061yc0p2cfgtn6

### До исправления:
```
Занятость: НЕТ ❌
Семейное положение: НЕТ ❌
Инфо о супруге: НЕТ ❌
Есть дети: НЕТ ❌
Хобби: НЕТ ❌
О себе: НЕТ ❌
```

### После исправления:
```
Занятость: WORK ✅
Семейное положение: MARRIED ✅
Инфо о супруге: Котлова Елена Юрьевна, поет песни ✅
Есть дети: true ✅
Инфо о детях: Рома (01.07.2000) ✅
Даты рождения детей: [{"name":"Рома","birthDate":"2000-07-01","gender":"М"}] ✅
Хобби: Играю на гитаре ✅
О себе: Люблю ковыряться в носу ✅
```

---

## 📋 Отображение в интерфейсе

Все дополнительные поля отображаются в разделе **Профиль** (`/dashboard/profile`):

### Раздел "Дополнительная информация":

```
┌─────────────────────────────────────────────────────┐
│ Дополнительная информация                           │
├─────────────────────────────────────────────────────┤
│                                                     │
│ Занятость: [Выберите]                              │
│ ⊙ Работа   ○ Учеба   ○ Пенсия                      │
│                                                     │
│ Семейное положение: [Выберите]                     │
│ ⊙ Не женат/Не замужем                              │
│ ○ Женат/Замужем                                    │
│ ○ В разводе                                        │
│ ○ Вдовец/Вдова                                     │
│ ○ В гражданском браке                              │
│                                                     │
│ Информация о супруге/супруге:                      │
│ [Имя, род занятий и другая информация...]          │
│                                                     │
│ ☐ У меня есть дети                                 │
│                                                     │
│ + Добавить награду                                  │
│ + Добавить обучение                                 │
│                                                     │
│ Хобби и увлечения:                                 │
│ [Ваши хобби, интересы и увлечения...]              │
│                                                     │
│ О себе:                                            │
│ [Расскажите о себе: характер, привычки...]         │
│                                                     │
│                         [Сохранить изменения]      │
└─────────────────────────────────────────────────────┘
```

---

## 🧪 Тестирование

### Тест 1: Заполнение через чат (основной сценарий)
1. Зарегистрироваться как новый пользователь
2. Пройти весь диалог с ботом (основные данные + дополнительная информация)
3. ✅ Проверить что все поля сохранились в профиле
4. ✅ Проверить что данные отображаются корректно

### Тест 2: Ручное редактирование в профиле
1. Открыть раздел "Профиль"
2. Заполнить дополнительную информацию вручную
3. ✅ Сохранить
4. ✅ Проверить что данные сохранились

### Тест 3: Переэкстракт для существующих пользователей
1. Для пользователей, которые уже прошли диалог ДО исправления
2. Запустить скрипт переэкстракта:
   ```bash
   npx tsx scripts/re-extract-profile.ts
   ```
3. ✅ Данные должны извлечься из истории чата и сохраниться

---

## ⚠️ Важные замечания

### 1. Дети: Формат JSON

Поле `childrenBirthDates` хранит данные в формате **JSON-массива**:

```json
[
  {
    "name": "Рома",
    "birthDate": "2000-07-01",
    "gender": "М"
  },
  {
    "name": "Анна",
    "birthDate": "2005-12-15",
    "gender": "Ж"
  }
]
```

**Зачем нужен этот формат?**
- 🎁 Автоматическая отправка подарков на дни рождения
- 📊 Аналитика по возрастным группам детей
- 🎉 Рассылка поздравлений

**Как парсить:**
```typescript
const children = JSON.parse(user.childrenBirthDates || '[]');
for (const child of children) {
  console.log(`${child.name} (${child.gender}): ${child.birthDate}`);
}
```

---

### 2. Гендер ребенка определяется автоматически

Используется функция `detectGenderByName()` из `lib/utils/genderDetector.ts`.

**Примеры:**
- "Рома" → "М" (мужское имя)
- "Роман" → "М"
- "Анна" → "Ж"
- "Мария" → "Ж"

**Важно:** Если имя редкое или неоднозначное, может определиться неправильно. В таком случае можно исправить вручную в БД или через интерфейс (будущая фича).

---

### 3. Обратная совместимость

✅ **Старые пользователи:**
- Если поля не заполнены → отображаются как пустые
- Можно заполнить вручную в профиле
- Можно запустить переэкстракт из истории чата

✅ **Новые пользователи:**
- Все поля заполняются автоматически через диалог с ботом
- Можно редактировать в любой момент в профиле

---

## 🔄 Миграция для существующих пользователей

Если у вас есть пользователи, которые уже прошли диалог, но данные не сохранились:

### Вариант 1: Массовый переэкстракт (автоматический)

Создать скрипт для всех пользователей:

```bash
npx tsx scripts/re-extract-all-profiles.ts
```

### Вариант 2: Ручное заполнение (через UI)

Пользователь может:
1. Перейти в раздел "Профиль"
2. Заполнить дополнительную информацию вручную
3. Сохранить

---

## ✅ Статус

**Исправлено:** ✅  
**Протестировано:** ✅ На тестовом пользователе  
**Готово к использованию:** ✅

---

**Дата:** 24 ноября 2025  
**Проблема решена:** Дополнительные поля профиля теперь корректно извлекаются и сохраняются из диалога с ботом

