# 🐛 Исправление навигации в сайдбаре

## Проблема
**Симптомы:** Невозможно переключиться на вкладку "Скидки" (или другие пункты меню) когда открыт чат или обращение.

**Причина:** Когда ChatMenu развёрнут и содержит много сессий чата и обращений, он занимает всё доступное пространство в сайдбаре. Другие пункты меню (Документы, Скидки, Профиль, Настройки) уходят за границу видимости внизу сайдбара. 

Пользователь не понимает, что нужно прокрутить вниз, чтобы увидеть остальные пункты меню, и думает что навигация заблокирована.

## Исправление

### До:
```tsx
{isExpanded && (
  <div className="ml-2 space-y-0.5 border-l border-gray-200 dark:border-gray-700 pl-2">
    {/* Все сессии чата и обращения */}
  </div>
)}
```

**Проблема:** Список не ограничен по высоте и может занять всё пространство сайдбара.

### После:
```tsx
{isExpanded && (
  <div className="ml-2 max-h-96 space-y-0.5 overflow-y-auto border-l border-gray-200 dark:border-gray-700 pl-2">
    {/* Все сессии чата и обращения */}
  </div>
)}
```

**Решение:** 
- `max-h-96` - максимальная высота 384px (24rem)
- `overflow-y-auto` - прокрутка появляется если контент превышает максимальную высоту

## Результат

✅ ChatMenu теперь ограничен по высоте (максимум 384px)  
✅ Если сессий много - внутри ChatMenu появляется прокрутка  
✅ Остальные пункты меню (Скидки, Профиль и т.д.) **всегда видны**  
✅ Пользователь может переключаться между разделами в любой момент  

## Тестирование

### Сценарий 1: Много обращений
1. Создать 10+ обращений
2. Развернуть ChatMenu
3. ✅ Внутри ChatMenu появляется прокрутка
4. ✅ Пункты "Скидки", "Профиль", "Настройки" видны внизу

### Сценарий 2: Навигация
1. Открыть любое обращение или чат
2. ✅ Можно кликнуть на "Скидки" без прокрутки
3. ✅ Можно кликнуть на "Профиль" без прокрутки
4. ✅ Можно кликнуть на "Настройки" без прокрутки

### Сценарий 3: Свёрнутый ChatMenu
1. Свернуть ChatMenu (кликнуть на заголовок)
2. ✅ Все пункты меню сразу видны
3. ✅ Навигация работает

## Технические детали

### Размер max-h-96:
- `max-h-96` = 384px = 24rem
- Это примерно 8-10 пунктов меню в ChatMenu
- Достаточно для большинства случаев
- Если нужно больше - появляется прокрутка

### Альтернативные варианты:
Если `max-h-96` слишком мало или слишком много:
- `max-h-64` (256px) - 5-7 пунктов
- `max-h-80` (320px) - 7-9 пунктов  
- `max-h-96` (384px) - 8-10 пунктов ← **текущий**
- `max-h-[32rem]` (512px) - 12-14 пунктов

### Адаптивность:
Можно сделать адаптивную высоту в зависимости от размера экрана:
```tsx
className="ml-2 max-h-64 md:max-h-80 lg:max-h-96 overflow-y-auto ..."
```

Но для текущей задачи `max-h-96` достаточно.

## Файлы изменены

- `components/dashboard/ChatMenu.tsx` - добавлено `max-h-96 overflow-y-auto`

## Перезапуск

Перезапустите сервер для применения изменений:
```bash
# В терминале где запущен pnpm dev
# Ctrl+C для остановки
pnpm dev
```

Или просто обновите страницу (Hot Reload должен применить изменения автоматически).

