# 🔍 Настройка Tavily API для веб-поиска в чатах

## ✅ Статус
Tavily API настроен и работает! Ключ добавлен в `.env.local`.

## 📋 Что было сделано

1. **Добавлен ключ API** в `.env.local`:
   ```bash
   TAVILY_API_KEY="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"
   ```

2. **Реализован расширенный поиск** (`lib/chat-enhanced-search.ts`):
   - Поиск в базе знаний (векторный поиск)
   - Поиск в БД организаций (председатели, контакты)
   - Поиск в интернете через Tavily API

3. **Интегрирован в чаты** (`app/api/chat/[chatId]/route.ts`):
   - Бот использует все источники информации перед ответом
   - При запросе об организации автоматически выполняется веб-поиск

## 🧪 Тестирование

Проверить работу можно через тестовый скрипт:
```bash
node scripts/test-tavily-search.mjs
```

## 🎯 Как это работает

Когда пользователь задаёт вопрос типа:
- "как зовут председателя профсоюза МООП РЗ РФ?"
- "кто руководит МООП РЗ?"

Бот автоматически:

1. **Ищет в базе знаний** (загруженные документы)
2. **Ищет в БД организаций** (информация о председателях из админки)
3. **Ищет в интернете** через Tavily API (если информации недостаточно)

## 📊 Пример работы

Тестовый запрос: "председатель МООП РЗ РФ профсоюз"

**Найденные результаты:**
- ✅ Председатель МООП РЗ РФ: **Суслонова Нина Владимировна**
- ✅ URL: https://www.mooprz.ru/
- ✅ Дополнительная информация из открытых источников

## ⚙️ Конфигурация

Ключ API хранится в `.env.local`:
```bash
TAVILY_API_KEY="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"
```

**Важно:** После изменения `.env.local` необходимо перезапустить сервер Next.js для применения изменений.

## 🔧 Устранение проблем

Если веб-поиск не работает:

1. Проверьте наличие ключа:
   ```bash
   grep TAVILY_API_KEY .env.local
   ```

2. Проверьте работу API:
   ```bash
   node scripts/test-tavily-search.mjs
   ```

3. Проверьте логи сервера на наличие ошибок:
   ```
   [enhanced-search] 🔍 Performing web search with Tavily API
   [enhanced-search] ✅ Found X web search results
   ```

## 📝 Логи

В консоли сервера будут видны логи поиска:
- `🔍 Performing web search with Tavily API` - начало поиска
- `✅ Found X web search results` - успешный поиск
- `❌ Error in web search` - ошибка поиска

