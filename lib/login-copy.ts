/**
 * Тексты подсказок на странице входа (для единого места правок и тестирования).
 */

/** Подсказка под полем телефона: куда отправится код (SMS / Telegram / MAX). */
export const LOGIN_INPUT_HINT_SMS =
  "Отправим код в SMS 📱 (или в Telegram / MAX, если привязаны)";

/** Подсказка под полем email. */
export const LOGIN_INPUT_HINT_EMAIL = "Отправим ссылку для входа на email ✉️";
